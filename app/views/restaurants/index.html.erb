<h1>Restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li>
      <%= link_to restaurant.name, restaurant_path(restaurant) %>
      <%= link_to "Destroy", restaurant_path(restaurant), remote: true, method: :delete %>
    </li>
  <% end %>
</ul>